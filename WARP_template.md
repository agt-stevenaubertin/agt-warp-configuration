# Project-Specific Warp Configuration Template

<!-- Copy this template to your project's root as `.warp/WARP.md` -->
<!-- These rules will only apply when working in that project directory -->

## Project Context

**Project Name:** [Project Name]  
**Language/Framework:** [e.g., C#/.NET, Python, TypeScript]  
**Purpose:** [Brief description of what this project does]

## Development Environment

- **Build Tool:** [e.g., dotnet, MSBuild, npm, cargo]
- **Package Manager:** [e.g., NuGet, npm, pip]
- **Test Framework:** [e.g., xUnit, NUnit, pytest, jest]
- **Linter/Formatter:** [e.g., Ruff, ESLint, Prettier]

## Common Commands

```bash
# Build
[your build command]

# Test
[your test command]

# Run
[your run command]

# Lint/Format
[your lint command]
```

## Code Style & Conventions

- Indentation: [spaces/tabs, size]
- Naming conventions: [PascalCase, camelCase, etc.]
- File organization: [describe structure]
- Comment style: [XML docs, JSDoc, etc.]

## AI Agent Instructions

When working on this project:

1. **Always** [specific instruction, e.g., "run tests before committing"]
2. **Never** [specific restriction, e.g., "modify generated files"]
3. **Prefer** [preference, e.g., "async/await over .Result"]
4. **Architecture notes:** [important architectural decisions]

## File Patterns to Avoid

- `bin/`, `obj/` - Build outputs
- `node_modules/` - Dependencies
- `*.Designer.cs` - Auto-generated files

## Special Considerations

[Any project-specific quirks, legacy code areas, sensitive modules, etc.]
